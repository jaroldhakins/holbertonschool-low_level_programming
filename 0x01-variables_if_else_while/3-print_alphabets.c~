#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)

{

char l, p;

p = 'a';
l = 'A';

while (p <= 'z')
{
putchar(p);
p++;

while (l <= 'Z')
{
putchar(l);
l++;
}
}

putchar("\n");

return (0);

}
